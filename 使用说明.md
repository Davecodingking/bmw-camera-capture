# 录制使用说明（给美术同学）

> 一句话：进游戏 → 开自由相机走位 → 按 **F8** 录一段 → 把生成的文件夹交给处理同学。
> 你只负责 **装好 + 走位录制**。看到**绿灯**就说明在正常录。

---

## 0. 你会用到的东西

| 东西 | 是什么 | 从哪来 |
|---|---|---|
| 游戏 | 黑神话悟空 / Expedition 33 等 **UE5 游戏** | 你自己有 |
| ReShade | 往游戏里注入的工具 | 官网 reshade.me（下**带 Add-on 支持**的版本） |
| **BMWCameraCapture.addon64** | 我们的录制插件（**已编译好，直接用**） | 本仓库 `prebuilt/` 文件夹 |
| UUU | 自由相机工具（Universal Unreal Unlocker） | 你现在用的那个 |

> ⚠️ 插件**不用你编译**，`prebuilt/` 里那个 `.addon64` 拿来直接用即可。

---

## 1. 一次性安装（每个游戏装一次）

### 1.1 装 ReShade
1. 官网下载 **带 Add-on 支持** 的 ReShade（安装时有个「with full add-on support」的选项，一定要选它）。
2. 运行安装器 → 选中游戏的 **exe** → 渲染 API 选 **DirectX 12**。
3. 着色器包可以全部不勾（我们不需要滤镜）。

### 1.2 放我们的插件
把仓库 `prebuilt/BMWCameraCapture.addon64` **复制到游戏 exe 所在的那个文件夹**（也就是和 ReShade 刚装出来的 `dxgi.dll` 放在一起）。

> 举例（你的游戏路径会不同）：
> `...\Expedition 33\Sandfall\Binaries\Win64\`
> 这个文件夹里应该同时能看到：`游戏.exe`、`dxgi.dll`、`BMWCameraCapture.addon64`。

### 1.3 准备好 UUU
UUU 照你平时的用法准备好即可（能在这个游戏里开自由相机就行）。

---

## 2. ⚠️ 游戏内画质设置（最重要，错了就录不上）

进游戏后，先把画质设成这三条，**否则会一直黄灯、录不出东西**：

| 设置 | 必须是 |
|---|---|
| 超分 / 抗锯齿 | **DLSS** |
| 分辨率缩放 / 内部分辨率 | **100%（原生，不要缩放）** |
| 帧生成 / Frame Generation | **关闭** |

> 为什么：换成 **XeSS** 或开 **帧生成**，插件抓不到相机 → 黄灯、不出文件。**只认 DLSS + 原生 + 关帧生成。**

---

## 3. 录制流程

1. 进游戏，走到你要录的**场景**（别在菜单/加载画面）。
2. 用 **UUU 开自由相机**（一般是按 **Insert**）。
3. 慢慢移动相机，对准要重建的目标。
4. 按 **F8 开始录制** → 角落出现 **🟢 绿灯**，同时有一声**升调提示音**。
5. 保持录制，**慢速**围着目标多角度走位（见下方「走位要点」）。
6. 按 **F8 停止** → **降调提示音**。
7. 退出自由相机 / 退出游戏。

### 看灯判断（记这一条就够）

| 灯 | 意思 | 怎么办 |
|---|---|---|
| 🟢 **绿灯** | 正在正常录制、在写文件 | 继续走位 |
| 🟡 **黄灯** | **没录上**（多半在菜单/加载，或画质设置不对） | 回到有正常画面的场景；确认第 2 节画质设置 |

> 按了 F8 一点反应都没有？先按 **Home** 看 ReShade 面板有没有出来（确认插件加载了），再按 F8。

---

## 4. 走位要点（决定重建质量）

- **慢、稳**，不要猛甩镜头（甩太快帧与帧之间对不上）。
- **围着目标转 + 缓慢平移**，多角度覆盖。
- 覆盖要**够多**（正式录建议几百帧，别就录两三秒）。
- 光照、天气尽量别在录制中途突变。

---

## 5. 录出来的东西 & 怎么交接

录完后，在**游戏 exe 那个文件夹**里会多出一个 `BMWCameraCapture` 文件夹：

```
BMWCameraCapture/
  bmw_camera_pos.csv     相机数据
  color/                 每帧彩色（.bin + 可直接看的 .bmp）
  depth/                 每帧深度（.bin + .bmp）
```

**把整个 `BMWCameraCapture` 文件夹打包，交给做后处理的同学。** 完事。

> 🔴 **重要**：每次重新录之前，把上一次的 `BMWCameraCapture` 文件夹**改个名或移走**（比如加个日期），否则新旧数据会混在一起。

---

## 6. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 一直黄灯、不出文件夹 | 画质没设对 → 改回 **DLSS + 原生100% + 关帧生成** |
| 按 F8 完全没反应 | 先按 **Home** 确认 ReShade 加载；确认 `.addon64` 放对了文件夹 |
| 用了 XeSS → 录不上 | 换回 **DLSS**（插件目前只认 DLSS 的渲染方式） |
| 游戏中途闪退 | 别一次录太久，**分几段短的**录（每段按一次 F8 起、一次 F8 停） |
| 换了新游戏 | 把 `.addon64` 复制到**新游戏**的 exe 目录即可（同一个文件通用，UE5 游戏基本都能用） |

---

## 7. （处理同学看）后处理

> 这部分给做后处理的同学，美术不用管。

**环境**：Python（3.12/3.13/3.14 均可）+ `opencv-python` + `ffmpeg`。`setup_and_run.sh` 会自动帮你装（需联网）。

**批量处理**：把每天/每次的采集目录按 `BMWCameraCapture_<名字或日期>` 放进一个 DATASET 根目录，然后：
```bash
bash tools/setup_and_run.sh  "D:/你的DATASET根目录"
```
每段会在 `<DATASET>/rgb_depth_dataset_<名字>/seg<N>/` 下产出：
- `rgb.mp4`（彩色）、`depth.mkv`（16bit 无损深度）
- `camera_params.json`（内外参 + 每帧相机位姿）
- `alignment.mp4`（逐帧对齐检查视频）

**出点云 / 3DGS splat**（可拖进 https://superspl.at/editor 看）：
```bash
python tools/dataset_to_ply.py --dataset-dir <上面的 seg 目录> --gaussian --per-frame --out <输出前缀> --stride 4
```

**想跳过视频、直接从原始 .bin 快速出点云**（只需 numpy）：
```bash
python tools/bin_to_splat.py --csv <BMWCameraCapture/bmw_camera_pos.csv> --out-dir splat_out --indices 0,2,5 --combined
```

**重建原理（一句话）**：用采集时记录的**投影矩阵 + view_to_world 矩阵**做反投影（通用、旋转平移都对）；`igcs_*` 列是 UUU 引擎相机，仅用来**剔除抓错相机的帧**（嗅探朝向和引擎相机朝向差 >3° 的帧自动丢弃），不参与几何计算。

---

## 8. （工程同学看）重新编译插件

> 只有改了插件源码 `addon/BMWCameraCapture.cpp` 才需要。美术/处理同学不用管。

需要 ReShade **6.7.3** 的头文件（API 18）+ 任意较新的 MSVC（VS2022/2026 都行）：
```bash
cmake -S addon -B addon/build -G "Visual Studio 17 2022" -A x64 \
      -DRESHADE_INCLUDE_DIR=<reshade-6.7.3/include 路径>
cmake --build addon/build --config Release
```
产物：`addon/build/Release/BMWCameraCapture.addon64`。

> 校准新引擎（可选）：万一以后某个游戏 F8 一直黄灯抓不到相机，把源码里 `kDumpRawCbvs` 改成 `true` 重新编译，录几秒会在 `BMWCameraCapture/cbv_dumps/` 存下常量缓冲原始字节，交给工程同学离线找出该游戏的相机偏移。默认关闭。
