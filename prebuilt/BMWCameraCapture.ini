# BMWCameraCapture 配置（把这个文件和 BMWCameraCapture.addon64 放在同一个文件夹，
# 也就是游戏 exe 那个目录）。改完保存即可，下次进游戏生效。

# 是否额外输出可直接看的预览图 .bmp（除了必需的 .bin 之外）：
#   save_bmp=1  ->  同时出 .bin + .bmp（能在文件夹里直接看缩略图，占空间大一些）
#   save_bmp=0  ->  只出 .bin（省空间、写盘更快，看不到缩略图）
# 不放这个文件 / 不写这行 = 默认 1（出 bmp）。
save_bmp=1
